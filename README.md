user-db-api/
├── config/
│   └── db.js          (Database connection)
├── routes/
│   └── userRoutes.js  (API logic with SQL)
├── server.js          (Entry point)
├── .env               (Store your DB credentials here)
└── .gitignore         (Tell Git to ignore .env and node_modules)
